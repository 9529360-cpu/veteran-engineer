<!-- veteran-overlay-tombstone -->
# Retired: Long-Running Engineering Execution

This path is retained only to neutralize an undeletable file from older Workspace plugin releases.

Do not route to or load it as active guidance. Long-running foreground execution, checkpointing, continuation, and host-boundary rules are maintained in the main `SKILL.md` and `chatgpt-web-host-execution.md`.

The explicit tombstone marker keeps this compatibility path visible to audits without treating it as active knowledge.
