"""Retired Workspace overlay compatibility tombstone.

This legacy test module was removed after its responsibilities were split into
the current focused routing, composition, behavior, and integrity tests. The
path remains intentionally import-safe and contains no tests because Workspace
plugin updates cannot delete files from older releases.
"""
